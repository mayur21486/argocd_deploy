apiVersion: v1
kind: Service
metadata:
  name: {{ include "gauravapp-dev.fullname" . }}
  labels:
    {{- include "gauravapp-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gauravapp-dev.selectorLabels" . | nindent 4 }}
